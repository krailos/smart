spring:
  application:
    name: smart
  datasource:
    url: jdbc:postgresql://localhost:5432/school
    username: admin
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    properties.hibernate:
      show_sql: true
      format_sql: true
logging:
  level:
    root: info
    com.krailo.smart.database.pool: info
  file:
    name: smart.log
    path: /


